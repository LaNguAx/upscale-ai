{
  "name": "upscale-ai",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=frontend... --filter=backend...",
    "dev:ai": "turbo run dev --filter=ai",
    "preview": "turbo run preview",
    "preview:web": "turbo run preview --filter=frontend... --filter=backend...",
    "preview:ai": "turbo run preview --filter=ai",
    "start:prod": "turbo run start:prod",
    "start:prod:web": "turbo run start:prod --filter=frontend... --filter=backend...",
    "start:prod:ai": "turbo run start:prod --filter=ai",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "prettier": "^3.7.4",
    "turbo": "^2.9.5",
    "typescript": "6.0.2"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  }
}
