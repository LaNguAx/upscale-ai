# Path to the trained model checkpoint (defaults to ./checkpoints/vsr_model_best.pth)
#CHECKPOINT_PATH=
# Inference device: cuda or cpu (auto-detected by default)
#DEVICE=cuda
# Inputs taller than this are downscaled before inference
MAX_INPUT_HEIGHT=480
HOST=0.0.0.0
PORT=8000
# Internal bearer token required for /process-upload, /result, and /cancel.
# Empty = no auth (local dev). Must match the backend's AI_INTERNAL_TOKEN.
AI_INTERNAL_TOKEN=
# Working directories for the remote transport (uploaded inputs and enhanced
# outputs), resolved relative to apps/ai.
WORK_UPLOAD_DIR=../../storage/ai/uploads
WORK_RESULT_DIR=../../storage/ai/results
