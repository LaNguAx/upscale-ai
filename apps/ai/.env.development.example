# Path to the trained model checkpoint (defaults to ./checkpoints/vsr_model_v4_best_codec_psnr.pth)
#CHECKPOINT_PATH=
# Inference device: cuda or cpu (auto-detected by default)
#DEVICE=cuda
# Maximum supported input height. Taller input is rejected, never downscaled.
MAX_INPUT_HEIGHT=480
HOST=0.0.0.0
PORT=8000
# Internal bearer token required for /process-upload, /result, and /cancel.
# Empty = no auth (local dev). Must match the backend's AI_INTERNAL_TOKEN.
AI_INTERNAL_TOKEN=
# Working directories for the remote transport (uploaded inputs and enhanced
# outputs), resolved relative to apps/ai.
WORK_UPLOAD_DIR=../../storage/ai/uploads
WORK_RESULT_DIR=../../storage/ai/results
# Progressive preview frames written during inference (sampled enhanced JPEGs).
PREVIEW_ENABLED=true
# Playback stride: 2 gives a 15 fps flipbook at a 30 fps source. Raise it
# (e.g. 15) to fall back to sparse still-frame sampling.
PREVIEW_EVERY_N_FRAMES=2
PREVIEW_MAX_WIDTH=640
PREVIEW_JPEG_QUALITY=80
WORK_PREVIEW_DIR=../../storage/ai/previews
